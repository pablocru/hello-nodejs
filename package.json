{
  "name": "hello-nodejs",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "workshops/**"
  ],
  "scripts": {
    "lint": "eslint",
    "test": "npm run test --workspaces --if-present",
    "test:verbose": "npm run test:verbose --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "globals": "^15.12.0",
    "typescript-eslint": "^8.15.0"
  }
}
